# 서브쿼리

서브쿼리는 메인 쿼리의 조건 부분(WHERE)에 포함된 쿼리입니다. 서브 쿼리가 먼저 계산되고 그곳에서 추출된 결과가 메인 쿼리에 적용됩니다. 

서브쿼리는 괄호로 묶어야 합니다.
ORDER BY 사용 불가

예제) <전공> 테이블로부터 학생이 최소 1명 이상인 레코드들을 조회하세요.
```sql
SELECT * FROM 전공
WHERE 전공ID IN (SELECT 전공ID FROM 학생);
```