# DML Practice

and, or 같이
전통적 오라클 조인
distinct + where 
count() 쓸 때 NULL 
count() + distinct
별칭 + 집계 함수
order + 집계함수
서브쿼리 응용
크로스 조인 + %


# AND + OR

```sql
SELECT * FROM 학생 
WHERE 학년 = 3 AND 학년 = 4 OR 
```

