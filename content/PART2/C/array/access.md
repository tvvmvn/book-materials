# 요소에 접근하기

배열의 요소에 접근하기 위해 요소의 고유한 인덱스(index)를 활용합니다. 첫번째 요소의 인덱스는 0이며 뒤로 갈수록 1씩 증가합니다.

```c
int myNumbers[] = {25, 50, 75, 100};

printf("%d", myNumbers[0]); // 25
printf("%d", myNumbers[1]); // 50
printf("%d", myNumbers[2]); // 75
printf("%d", myNumbers[3]); // 100
```