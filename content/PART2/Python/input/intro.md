# 사용자 입력

input 함수는 사용자의 입력을 처리하고 입력값을 반환합니다

```py
print("이름이 뭐니?")

name = input()

print("안녕 " + name)
```

input 함수는 다음과 같이 인자를 가질 수 있습니다. 인자는 사용자 입력에 앞서 출력됩니다.

```py
name = input("이름이 뭐니?")

print("안녕 " + name)
```
