# 서브쿼리

서브쿼리는 메인 쿼리의 조건 부분(WHERE)에 포함된 쿼리입니다. 서브 쿼리가 먼저 계산되고 그곳에서 추출된 결과가 메인 쿼리에 적용됩니다. 서브쿼리는 보통 괄호로 묶습니다.

예제)
다음은 학생이 최소 1명 이상인 동아리 레코드들을 출력하는 명령문입니다.
```sql
SELECT * FROM 동아리
WHERE 동아리ID IN (SELECT 동아리ID FROM 학생);
```