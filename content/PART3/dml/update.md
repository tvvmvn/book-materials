# UPDATE 문

UPDATE문은 레코드를 수정할 때 사용되는 명령어입니다. 조건을 제시하지 않으면 테이블의 모든 컬럼이 수정되니 주의하세요. 

```sql
UPDATE 테이블명 SET 컬럼1 = 값1, 컬럼2 = 값2, ... WHERE 조건;
```

예제)
다음은 '학생ID'가 10인 레코드의 '취미'를 '야구'로 수정하는 명령문입니다.
```sql
UPDATE 학생 SET 취미 = '야구' WHERE 학생ID = 10;
```
